#ifndef ROAD_NETWORK_H
#define ROAD_NETWORK_H

#include <string>
#include <vector>
#include <list>
#include <iostream>
#include <assert.h>

class RoadNetwork
{
public:
	RoadNetwork();
	RoadNetwork(const RoadNetwork& aRoadNetwork);
	virtual ~RoadNetwork();
};
#endif
